import React from "react";
import styled from "styled-components";

// select 태그 선택지 부분 컴포넌트입니다
// value, index, style 을 받아요
const DropDownMenu = ({ value, index, style }) => {
  return <div>{value}</div>;
};

export default DropDownMenu;
